/* import { useReducer } from "react"

enum actions{
FETCH = 'FETCH'
}
interface Action{
    type: actions,
    payload?:string
}
interface State{

}
const fetchReducer = (state:State,action:Action)=>{

    switch(action.type){
        case actions.FETCH:{
            return 
        }
    }

}

 */